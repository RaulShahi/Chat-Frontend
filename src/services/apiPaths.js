import { API_PATH } from "../helpers/api";

// paths for chats
const API_PATH_CHAT = `${API_PATH}chats`;
const API_PATH_USERS = `${API_PATH}users`;

export const Apis = {
  chats: API_PATH_CHAT,
  users: API_PATH_USERS,
};
